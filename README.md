# GitHub Webhook Receiver

Flask application that receives GitHub webhooks and stores events in MongoDB Atlas.

## Features

- Receives GitHub webhooks for Push, Pull Request, and Merge events
- Stores events in MongoDB Atlas
- Real-time UI that polls MongoDB every 15 seconds
- Displays events in the required format

## Setup

1. Install dependencies:
pip install -r requirements.txt
